const QUERIES={
 identity:`SELECT json_build_object('customersTotal',count(*),'cpfValid',count(*) FILTER (WHERE length(regexp_replace(coalesce(cpf,''),'\\D','','g'))=11),'cpfMissingInvalid',count(*) FILTER (WHERE length(regexp_replace(coalesce(cpf,''),'\\D','','g'))<>11),'withPersonKey',count(*) FILTER (WHERE person_key IS NOT NULL AND person_key<>''),'withPersonId',count(*) FILTER (WHERE person_id IS NOT NULL)) value FROM customers`,
 people:`SELECT json_build_object('total',(SELECT count(*) FROM persons),'multiCustomerPeople',(SELECT count(*) FROM (SELECT person_id FROM customers WHERE person_id IS NOT NULL GROUP BY person_id HAVING count(*)>1)x),'duplicateCpfGroups',(SELECT count(*) FROM (SELECT regexp_replace(cpf,'\\D','','g') FROM customers WHERE length(regexp_replace(coalesce(cpf,''),'\\D','','g'))=11 GROUP BY 1 HAVING count(*)>1)x),'duplicatePersonKeyGroups',(SELECT count(*) FROM (SELECT person_key FROM customers WHERE person_key IS NOT NULL AND person_key<>'' GROUP BY 1 HAVING count(*)>1)x)) value`,
 coverage:`SELECT json_build_object('orders',json_build_object('total',(SELECT count(*) FROM orders),'linked',(SELECT count(*) FROM orders WHERE person_id IS NOT NULL)),'pdvSales',json_build_object('total',(SELECT count(*) FROM pdv_sales),'linked',(SELECT count(*) FROM pdv_sales WHERE person_id IS NOT NULL)),'parcelas',json_build_object('total',(SELECT count(*) FROM crediario_parcelas),'linked',(SELECT count(*) FROM crediario_parcelas WHERE person_id IS NOT NULL)),'baixas',json_build_object('total',(SELECT count(*) FROM crediario_baixas),'linked',(SELECT count(*) FROM crediario_baixas WHERE person_id IS NOT NULL)),'marcados',json_build_object('total',(SELECT count(*) FROM marcados),'linked',(SELECT count(*) FROM marcados WHERE person_id IS NOT NULL))) value`,
 financial:`SELECT json_build_object('pdvFinalized',json_build_object('count',(SELECT count(*) FROM pdv_sales WHERE status='finalized' AND NOT is_training),'amount',(SELECT round(coalesce(sum(total),0)::numeric,2)::text FROM pdv_sales WHERE status='finalized' AND NOT is_training)),'parcelas',json_build_object('count',(SELECT count(*) FROM crediario_parcelas),'amount',(SELECT round(coalesce(sum(valor_parcela),0)::numeric,2)::text FROM crediario_parcelas)),'baixas',json_build_object('paid',json_build_object('count',(SELECT count(*) FROM crediario_baixas WHERE status='paid'),'amount',(SELECT round(coalesce(sum(total_pago),0)::numeric,2)::text FROM crediario_baixas WHERE status='paid')),'pending',json_build_object('count',(SELECT count(*) FROM crediario_baixas WHERE status='pending'),'amount',(SELECT round(coalesce(sum(total_pago),0)::numeric,2)::text FROM crediario_baixas WHERE status='pending')),'canceled',json_build_object('count',(SELECT count(*) FROM crediario_baixas WHERE status='canceled'),'amount',(SELECT round(coalesce(sum(total_pago),0)::numeric,2)::text FROM crediario_baixas WHERE status='canceled'))),'marcados',json_build_object('count',(SELECT count(*) FROM marcados),'amount',(SELECT round(coalesce(sum(valor_total),0)::numeric,2)::text FROM marcados))) value`
}; module.exports={QUERIES};
