{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "regions": [
    "gru1"
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':/ecommerce' && { ! git rev-parse -q --verify HEAD^2 >/dev/null || git diff --quiet HEAD^2 HEAD -- ':/ecommerce'; }"
}
