'use client';

/** /retaguarda/rh/operadores — MATRIZ (vê todas as lojas). Ver OperadoresManager. */
import OperadoresManager from '@/components/rh/OperadoresManager';

export default function OperadoresPage() {
  return <OperadoresManager backHref="/retaguarda/rh" backLabel="RH" />;
}
