{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": [
    "gru1"
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- ':/frontend' && { ! git rev-parse -q --verify HEAD^2 >/dev/null || git diff --quiet HEAD^2 HEAD -- ':/frontend'; }"
}
